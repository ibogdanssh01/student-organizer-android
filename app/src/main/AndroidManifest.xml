<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.StudentOrganizer">

        <!-- MAIN ACTIVITY (LAUNCHER) -->
        <activity
            android:name=".activities.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- OTHER ACTIVITIES -->
        <activity android:name=".activities.CourseListActivity" />
        <activity android:name=".activities.CourseFormActivity" />
        <activity android:name=".activities.AssignmentListActivity" />
        <activity android:name=".activities.AssignmentFormActivity" />
        <activity android:name=".activities.ExamListActivity" />
        <activity android:name=".activities.ExamFormActivity" />
        <activity android:name=".activities.ScheduleActivity" />
        <activity android:name=".activities.DashboardActivity" />

    </application>

</manifest>